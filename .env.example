# Zero-cost deterministic mode is the production default.
AI_PROVIDER=none

# Optional server-only explanation provider. The core agent never needs these.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
